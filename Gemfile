source "https://rubygems.org"

# Minimum set for local builds via bundler (optional for your current flow)
gem "jekyll", "~> 4.4"

group :jekyll_plugins do
  gem "jekyll-paginate", "~> 1.1"
  # Enable when you add to _config.yml plugins
  gem "jekyll-feed", "~> 0.17", require: false
  gem "jekyll-sitemap", "~> 1.4", require: false
  # gem "jekyll-seo-tag", "~> 2.8", require: false
end

